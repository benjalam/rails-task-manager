<h1>Tasks</h1>
<%= link_to "Add a Task", new_task_path %>
<% @tasks.each do |task| %>
  <h2><%=task.name%></h2>
  <p><%=task.description%></p>
  <p><%=task.done%></p>
  <h3><%= link_to task.name, task_path(task) %></h3>

<% end %>


